/**
 * Shared design tokens used by both light and dark themes.
 * Reference these in component overrides to maintain a single source of truth.
 */
export const sharedTokens = {
  borderRadius: {
    button: 8,
    input: 6,
  },
  focusRingWidth: 3,
} as const;

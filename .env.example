# Playwright MCP server — URL of the running Storybook instance.
# Used by the @playwright/mcp server configured in .mcp.json to open the browser
# against the correct Storybook URL.
# Default: http://localhost:3000 (matches `npm run dev`)
PLAYWRIGHT_STORYBOOK_URL=http://localhost:3000
